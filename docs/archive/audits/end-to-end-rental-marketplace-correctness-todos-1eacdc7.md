---
status: archived
owner: engineering
last_reviewed: 2026-05-08
source_of_truth: false
---

# End-to-End Rental Marketplace Correctness TODOs

**Repository:** `samujjwal/rental`  
**Target commit:** `1eacdc7a100f1d7dae90dfae65718167e97fa12d`  
**Deliverable focus:** Detailed TODO list only, derived from the tailored ultra-strict rental marketplace audit prompt.  
**Static review note:** This TODO list is based on static repository inspection through the connected GitHub tool. Tests were not executed in this environment.

---

## Priority Legend

- **P0 — Must fix before production:** correctness/security/data/payment/booking blocker.
- **P1 — Must fix before release:** important visible or operational gap.
- **P2 — Hardening:** maintainability, consistency, observability, performance.
- **P3 — Future enhancement:** non-blocking improvement.

---

## P0 — Must Fix Before Production

| ID | Area | File(s) | Issue | Required Fix | Acceptance Criteria | Required Tests |
|---|---|---|---|---|---|---|
| TODO-P0-001 | Availability / Booking Safety | `apps/api/src/modules/listings/services/availability.service.ts` | `checkAvailability()` catches failures from the availability-rule lookup and existing-booking lookup, logs them, and continues toward `isAvailable: true`. This can create a false available result when DB/query logic fails. | Fail closed for availability and booking conflict checks. If booking conflict lookup fails, return a 503/500 recoverable error instead of `available: true`. Do not allow checkout or booking creation to proceed based on skipped critical safety checks. | Any DB error during availability or booking conflict lookup blocks the availability check and does not return `available: true`. UI/mobile receives actionable error state. | Unit test for availability DB failure. API E2E where booking conflict query fails and endpoint returns failure. Web/mobile test verifies user cannot proceed to booking on availability-check failure. |
| TODO-P0-002 | Availability / Authorization | `apps/api/src/modules/listings/controllers/listings.controller.ts`, `apps/api/src/modules/listings/services/availability.service.ts` | Listing availability mutation endpoints require JWT but do not pass `userId` into the service and do not visibly enforce listing owner/admin/organization-member authorization before creating/updating availability. | Add server-side owner/admin/org authorization to create/update/delete/bulk availability operations. Pass authenticated user context into service. Validate listing ownership and org role before mutation. | Renter or unrelated owner cannot create, update, delete, or bulk-edit another listing's availability. Admin/operator access is explicitly role-gated. | API permission E2E for owner, unrelated owner, renter, org member, admin. Unit tests for authorization helper. |
| TODO-P0-003 | Availability / Type Safety / Data Integrity | `apps/api/src/modules/availability/repositories/availability.repository.ts` | Latest commit uses `(update as any).propertyId` inside `bulkUpdateAvailability()`, weakening type safety in a critical availability path and allowing updates without guaranteed listing scope. | Replace `Prisma.AvailabilityUpdateInput[]` with a strict input type that separates `propertyId` from mutable fields. Reject updates without a validated `propertyId`; never cast with `any`. | Bulk updates cannot compile or run unless each item has an explicit listing/property ID and permitted update fields only. | Type-level test or build guard. Unit tests for valid/invalid bulk update inputs. Integration test proves only scoped records are updated. |
| TODO-P0-004 | Availability / Conflict Correctness | `apps/api/src/modules/bookings/services/bookings.service.ts`, `apps/api/src/modules/listings/services/availability.service.ts` | Booking creation has its own conflict query and does not use the dedicated `checkAndReserve()` path. There are two availability/conflict sources of truth. | Create one canonical availability reservation service used by booking creation, booking cancellation, payment timeout, owner blocking, and search/date filtering. Booking creation must atomically check and reserve/confirm/release slots through this canonical service. | No duplicated overlap logic. Booking creation, cancellation, payment failure, and checkout timeout consistently reserve/release inventory. | Concurrent booking E2E with two renters for same listing/date. Unit tests for canonical overlap logic. Integration test for reservation release on cancellation/payment failure. |
| TODO-P0-005 | Booking / Date Overlap | `apps/api/src/modules/bookings/services/bookings.service.ts` | Booking conflict query uses inclusive overlap checks for both start and end boundaries. For rental periods modeled as `[startDate, endDate)`, a booking ending on Monday can incorrectly block another starting Monday. | Define canonical interval semantics in docs and code. Use `existing.startDate < requestedEnd && existing.endDate > requestedStart` consistently for half-open intervals, or explicitly document and enforce inclusive date semantics everywhere. | Back-to-back non-overlapping rentals are either allowed or consistently rejected according to documented business rule. Search, availability, booking, blocked dates, and UI calendars agree. | Unit tests for adjacent, overlapping, contained, enclosing, same-day, and invalid ranges. API E2E for back-to-back bookings. |
| TODO-P0-006 | Payment / Webhook Contract | `packages/database/prisma/schema.prisma`, `apps/api/src/modules/payments/webhook.service.ts`, `apps/api/src/modules/payments/services/stripe.service.ts` | Schema defines canonical `paymentIntentId` and deprecated `stripePaymentIntentId`; webhook handlers search by `stripePaymentIntentId`, while newer code may store/use `paymentIntentId`. This can cause Stripe webhooks to miss payment records. | Standardize on `paymentIntentId` as canonical. Update all reads/writes/webhook lookups to use the same field. Migrate deprecated fields safely or write both during migration. | `payment_intent.succeeded`, `payment_intent.payment_failed`, refunds, and charge events always locate the intended payment record. Deprecated field no longer causes missed state transitions. | Payment webhook integration tests for success, failure, cancellation, partial refund, full refund, and duplicate event using records with canonical `paymentIntentId`. |
| TODO-P0-007 | Payment / Test Bypass Isolation | `apps/api/src/modules/payments/services/stripe.service.ts`, `README.md`, env/scripts | `STRIPE_TEST_BYPASS` is guarded in several places, but the README documents isolated/manual flows that enable payment bypass. Production readiness requires a stronger global guardrail so bypass cannot leak through env/config drift. | Add a central startup validation that rejects payment bypass unless `NODE_ENV` is exactly `test` or `e2e`, and include a CI guard scanning production env examples/deploy configs for bypass flags. | App fails fast if bypass is enabled in dev/staging/prod. Production env templates cannot contain bypass flags. | Config unit tests. Startup integration test. CI guardrail test. |
| TODO-P0-008 | Payments / Deposit State Consistency | `apps/api/src/modules/payments/services/stripe.service.ts`, `apps/api/src/modules/bookings/services/booking-state-machine.service.ts`, `packages/database/prisma/schema.prisma` | Deposit flow uses mixed statuses such as `AUTHORIZED`, `HELD`, and `CAPTURED`. Comments mention “HELD means AUTHORIZED,” and dispute logic freezes deposits by setting `CAPTURED`, which can blur actual Stripe capture state. | Define a canonical deposit lifecycle: requested, authorized, held, released, captured, disputed/frozen, expired. Update enum, service logic, webhook handling, and UI labels accordingly. | Deposit status always represents actual money movement and operational state. Dispute freeze does not masquerade as Stripe capture unless funds were actually captured. | Unit tests for deposit hold/release/capture/freeze. Stripe webhook tests for deposit lifecycle. UI state tests for labels/actions. |
| TODO-P0-009 | Web/API Contract | `apps/api/src/modules/bookings/controllers/bookings.controller.ts`, `apps/web/app/lib/api/bookings.ts` | API returns paginated objects for `GET /bookings/my-bookings` and `GET /bookings/host-bookings`, while web client methods type them as `Promise<Booking[]>`. | Align contract. Either change API to return arrays if intended, or update web client to consume `{ data, total, page, limit }`. Prefer paginated object with typed shared contract. | Web booking pages render correct data, pagination, empty state, and errors. TypeScript types match runtime response. | Contract test for web client. API E2E snapshot/schema test. UI flow test for renter and owner bookings. |
| TODO-P0-010 | Mobile/API Contract | `apps/api/src/modules/bookings/controllers/bookings.controller.ts`, `apps/mobile/src/api/client.ts` | Mobile client also types `getMyBookings()` and `getHostBookings()` as raw `BookingSummary[]`, while API returns paginated objects. Mobile screens may break or silently render empty states. | Update mobile client and screens to consume paginated response, or normalize client result consistently. Keep shared response types across web and mobile. | Mobile bookings and host bookings screens correctly render API data and pagination. | Mobile unit test for client normalization. Mobile screen test. API contract test shared by web/mobile. |
| TODO-P0-011 | Search / Data Isolation and Cache Correctness | `apps/api/src/modules/search/services/search.service.ts` | Search cache key uses `JSON.stringify(searchQuery, Object.keys(searchQuery).sort())`, which only preserves listed keys and can drop nested filter/location/date values. Different searches can collide and return incorrect marketplace results. | Implement stable deep serialization for cache keys. Include all nested values deterministically: query, category, location, dates, filters, sort, pagination, cursor. | Search cache key differs for different cities, dates, filters, price ranges, coordinates, and radius. No stale/cross-filter result leakage. | Unit test for deep cache key uniqueness. Integration test with same query but different dates/location/filters. |
| TODO-P0-012 | Listing / Route Correctness | `apps/api/src/modules/listings/controllers/listings.controller.ts` | `@Get(':id')` appears before `@Get('nearby')`. In route frameworks where declaration order controls matching, `/listings/nearby` can be captured as `id = "nearby"`, breaking nearby search. | Move all static routes such as `nearby`, `featured`, `price-suggestion`, `slug/:slug`, `my-listings` before dynamic `:id`. Add route conflict tests. | `GET /listings/nearby` reliably invokes nearby handler and never `findById`. | API E2E for every static listing route and dynamic `:id` route. |
| TODO-P0-013 | Listing Media / Upload Safety | `apps/api/src/modules/listings/controllers/listings.controller.ts`, `apps/mobile/src/api/client.ts` | Listing `uploadImages` endpoint accepts URL arrays in JSON; mobile uploads files to `/upload/images`. The listing endpoint appends arbitrary URLs to `photos` without visible URL ownership, content-type, size, scan, storage, or authorization validation at this layer. | Require media uploads through a verified media service. Store media IDs/URLs only after ownership, MIME, size, virus/content-safety, and listing-owner checks. | Users cannot attach arbitrary external URLs or another user's media to a listing. Invalid media is rejected. | API E2E for upload/attach/delete ownership. Security test for arbitrary external URL injection. Mobile/web upload flow tests. |
| TODO-P0-014 | Production Readiness / Documentation Truth | `README.md` | README claims many production-grade features are complete, but the same file still lists critical next steps like external services, complete test suite, frontend routes, load/security testing, staging, and monitoring. This creates release-risk ambiguity. | Split shipped, partially shipped, demo/test, and future features into a current status matrix. Gate release claims against passing tests and configured integrations. | Docs accurately state what works at target commit. No contradictory “complete” vs “critical next step” claims. | Documentation lint/checklist in CI. Release readiness checklist requires current test evidence. |

---

## P1 — Must Fix Before Release

| ID | Area | File(s) | Issue | Required Fix | Acceptance Criteria | Required Tests |
|---|---|---|---|---|---|---|
| TODO-P1-001 | Availability / Performance | `apps/api/src/modules/listings/services/availability.service.ts` | `getAvailableDates()` calls `checkAvailability()` once per day, causing N+1 DB behavior over long ranges. | Replace per-day checks with batched booking/availability queries and deterministic in-memory calendar computation. | A 365-day range uses bounded queries, not 365 repeated booking checks. | Performance test for 30/90/365-day ranges. Unit test for generated availability calendar. |
| TODO-P1-002 | Availability / Hardcoded Currency | `apps/api/src/modules/listings/services/availability.service.ts` | `checkAndReserve()` creates `availabilitySlot` with hardcoded `currency: 'USD'`. | Use listing currency from DB or pricing context. Require currency input only from validated listing. | Availability slots match listing/booking currency. No hardcoded marketplace currency. | Unit/integration test for USD, NPR, EUR, INR listing currencies. |
| TODO-P1-003 | Availability / Raw SQL Safety | `apps/api/src/modules/listings/services/availability.service.ts`, `apps/api/src/modules/bookings/services/bookings.service.ts` | Advisory locks use `$executeRawUnsafe`. Even with parameter values, this should be replaced with safe raw APIs and centralized. | Replace with `$executeRaw` tagged template or a tested helper that validates inputs and avoids string construction. | No `$executeRawUnsafe` in production booking/availability paths. | Static guardrail test. Concurrent booking integration test. |
| TODO-P1-004 | Search / Cursor Pagination | `apps/api/src/modules/search/services/search.service.ts` | Cursor helpers exist but search still primarily uses offset pagination; cursor input does not appear to constrain the Prisma query. | Implement real cursor pagination or remove cursor fields from response/API until supported. | `cursor` changes result window deterministically; `hasMore` and `nextCursor` are correct. | API tests for first, next, final page using cursor; no duplicates/missing records. |
| TODO-P1-005 | Search / Geo Pagination Total | `apps/api/src/modules/search/services/search.service.ts` | Geo search fetches `size * 5` records and computes `total` from the filtered subset, not full matching population, so totals/hasMore can be wrong. | Use a scalable geospatial query or count all filtered candidates. Avoid partial fetch total as authoritative total. | Geo search total and hasMore are accurate across large datasets. | Integration test with > `size * 5` nearby listings. |
| TODO-P1-006 | Search / SQL Scalability | `apps/api/src/modules/search/services/search.service.ts` | Text search uses ILIKE/raw SQL fallback rather than the declared PostgreSQL full-text + pgvector strategy. | Use indexed full-text search and semantic ranking consistently; ensure ranking, filters, pagination, and availability compose correctly. | Search latency and ranking remain stable on realistic datasets. | Search integration tests, performance test, explain-plan/index assertions. |
| TODO-P1-007 | Booking / Manual Review State | `apps/api/src/modules/bookings/services/bookings.service.ts` | If tax/constraint/safety checks fail non-fatally, booking is created with metadata flags but no explicit workflow state or admin review queue is visible. | Add explicit manual-review state/queue and prevent payment/confirmation until required review is cleared. | Bookings with failed safety/tax/constraint checks cannot proceed silently. Admin sees and resolves review task. | API E2E for failed policy/tax/safety check. Admin review UI/API tests. |
| TODO-P1-008 | Booking / Price Breakdown Reliability | `apps/api/src/modules/bookings/services/bookings.service.ts` | Persisting price breakdown inside booking creation transaction catches and ignores errors, allowing booking creation without auditable breakdown. | Treat price breakdown as required for booking/payment audit or move to compensating retry with visible degraded state. | Every booking has complete persisted price lines before checkout/payment, or is blocked with recoverable error. | Integration test where breakdown persistence fails. Accounting audit test. |
| TODO-P1-009 | Payment / Escrow Idempotency | `apps/api/src/modules/payments/webhook.service.ts` | Payment success handler creates/funds escrow after checking ledger idempotency, but escrow creation idempotency is not clearly guarded in the inspected code. Duplicate Stripe events after cache expiry could create duplicate escrow unless DB/service enforces uniqueness. | Enforce unique escrow per booking/payment and idempotent `createEscrow/fundEscrow`. | Duplicate payment success webhooks do not duplicate escrow, ledger, or state history. | Webhook duplicate/replay integration test after idempotency cache expiry. |
| TODO-P1-010 | Payment / Owner Payout Validity | `apps/api/src/modules/bookings/services/booking-state-machine.service.ts` | Settlement creates payout using `Number(booking.ownerEarnings)` and owner Stripe account from listing owner. It does not visibly fail closed when owner earnings or Connect account is missing. | Validate owner earnings, currency, and Connect status before creating payout. Put booking into payout-review state if invalid. | No payout job with `NaN`, zero/negative invalid values, missing currency, or missing Connect account. | Unit tests for missing/invalid payout prerequisites. Payout processor integration test. |
| TODO-P1-011 | Notification Semantics | `apps/api/src/modules/bookings/services/booking-state-machine.service.ts` | Payment failure notification uses `NotificationType.BOOKING_CANCELLED`, which can confuse users and downstream notification filters. | Add/use `PAYMENT_FAILED` or equivalent notification type. Update templates and clients. | Renter sees clear “payment failed/retry” notification, not cancellation unless booking is actually cancelled. | Notification unit tests and UI/mobile notification rendering tests. |
| TODO-P1-012 | Listing / DTO Casting | `apps/api/src/modules/listings/controllers/listings.controller.ts` | Listing update casts `dto as any`, bypassing compile-time DTO contract in a core content path. | Remove `any` cast by aligning `UpdateListingDto` with service input type or introducing a mapper. | Listing update compiles with strict DTO/service types. | Typecheck and unit test for every update field. |
| TODO-P1-013 | Listing / Query Parsing | `apps/api/src/modules/listings/controllers/listings.controller.ts` | `page`, `limit`, lat/lng/radius, and boolean `all` query parameters are not consistently transformed/validated. For example `all === true` will be false for string `"true"`. | Use DTOs with class-transformer/class-validator or explicit parsing pipes for all query params. | Query params are parsed consistently and invalid values return 400. | API E2E for pagination, all=true, invalid lat/lng/radius, limit caps. |
| TODO-P1-014 | Listing / Owner Filtering | `apps/mobile/src/api/client.ts`, `apps/api/src/modules/listings/controllers/listings.controller.ts` | Mobile `getUserListings(userId)` calls `/listings?ownerId=...`, but `ListingsController.findAll()` docs/filters do not visibly include ownerId in query contract. | Add explicit `ownerId` filter support or implement a dedicated public user listings endpoint. | User profile listing sections load only the intended user's public available listings. | API contract test and mobile profile screen test. |
| TODO-P1-015 | Mobile / Error Shape | `apps/mobile/src/api/client.ts` | Mobile client throws raw `response.text()` for failed requests, losing structured API error fields and producing non-actionable messages. | Parse JSON error envelopes when available. Normalize error code/message/field errors for UI. | Mobile forms display actionable validation/auth/payment errors. | Client unit tests for JSON error, text error, 401 refresh failure, timeout. |
| TODO-P1-016 | Mobile / Request Cancellation | `apps/mobile/src/api/client.ts` | Generic requests have a 15s abort, but `authenticatedFetch()` does not use timeout/abort and can hang. | Centralize request helper so all calls have timeout, abort, refresh, and normalized errors. | No mobile API path bypasses timeout/refresh/error normalization. | Unit tests around timeout and refresh for both client and authenticatedFetch paths. |
| TODO-P1-017 | Build Scripts / Env Commands | `package.json`, `scripts/env/start-env.sh` | `env:start`, `env:stop`, `env:restart`, `env:status`, and `env:logs` all call `./scripts/env/start-env.sh` with no action argument, so they all default to start. | Pass proper action args: `dev start`, `dev stop`, `dev restart`, etc., or split wrapper scripts. | `pnpm env:stop` stops services; `env:status` shows status; `env:logs` tails logs. | Script smoke tests or CI shell tests for each command. |
| TODO-P1-018 | Test Coverage / Skipped Critical Tests | `apps/api/src/modules/bookings/services/*.spec.ts.skip`, `apps/api/src/modules/availability/services/*.spec.ts.skip`, `apps/api/src/security/*.spec.ts.skip` | Critical booking/payment/availability/security tests exist as `.skip` files according to repository search results. Critical correctness cannot rely on skipped specs. | Re-enable, repair, or replace skipped critical tests. Track any intentionally deferred tests in a release-blocking test debt register. | No critical-path `.spec.ts.skip` remains for booking, availability, payment integration, state machine, or security. | CI fails if critical-path tests are skipped. |

---

## P2 — Hardening / Maintainability

| ID | Area | File(s) | Issue | Required Fix | Acceptance Criteria | Required Tests |
|---|---|---|---|---|---|---|
| TODO-P2-001 | Domain Types | `apps/web/app/types/booking.ts`, `packages/shared-types/src/booking.ts`, API DTOs | Booking types appear duplicated across web, shared types, and API DTOs. | Move canonical user-facing booking contracts into shared package and generate/consume API types from OpenAPI where possible. | Web/mobile/API compile against the same contract for booking status, transitions, price, and condition reports. | Typecheck plus contract tests. |
| TODO-P2-002 | Status Mapping | `apps/web/app/features/bookings/detail/booking-status-config.ts`, `apps/api/src/modules/bookings/services/booking-state-machine.service.ts`, shared types | Booking status labels/actions likely duplicated between frontend config and backend state machine. | Publish state-machine metadata/available transitions from backend and keep client status config display-only. | UI actions come from backend transition eligibility, not duplicated client assumptions. | UI tests for role/status action matrix. |
| TODO-P2-003 | Logging / Sensitive Data | Payment, booking, auth, media, notification services | Logs include operational error messages. Review for PII/secrets/payment metadata exposure, especially webhook payloads, user IDs, email/phone, and payment intent IDs. | Add logging redaction policy and structured logger wrapper. | Logs include correlation IDs and redact secrets/PII. | Security log redaction tests. |
| TODO-P2-004 | Observability | Payment webhooks, booking state transitions, availability reservation, notifications | Critical marketplace flows need metrics/traces tied to booking/payment/listing IDs. | Add metrics for booking created/failed, double-booking prevented, webhook processed/retried/DLQ, notification delivered/failed, payout/refund lifecycle. | Operators can diagnose each critical flow from metrics/logs/traces. | Observability unit tests where feasible and runbook validation. |
| TODO-P2-005 | Documentation | `docs/product/*`, `README.md`, audit docs | Docs should trace each marketplace feature to implementation and test evidence. | Add feature status matrix with columns: shipped, route/screen, API, DB, tests, known gaps. | Product docs no longer overstate completeness. | Documentation checklist in CI. |
| TODO-P2-006 | API Response Consistency | Controllers across bookings/listings/search/notifications/disputes | API responses mix raw arrays, paginated objects, mapped frontend shapes, and domain models. | Introduce response DTOs and consistent pagination/error envelope. | Web/mobile clients consume predictable contracts. | API contract tests for each public endpoint. |
| TODO-P2-007 | Config Safety | `apps/api/src/config/configuration.ts`, env examples, scripts | Config flags are spread across scripts/docs/services. | Centralize config validation with required/forbidden env vars by environment. | App fails fast for unsafe/missing production config. | Config validation tests. |
| TODO-P2-008 | Raw Metadata JSON | Prisma models and services storing `metadata`, `damages`, `checklistData`, `rules` as strings | Several flows store structured data in string fields, increasing parse errors and contract drift. | Convert critical structured metadata to JSON fields where supported or add typed serializers/validators. | Metadata parse failures do not silently hide operational state. | Serializer unit tests and migration tests. |
| TODO-P2-009 | Search Ranking | `apps/api/src/modules/search/services/search.service.ts` | Relevance scoring, token fallback, semantic fallback, and sorting are mixed in one service. | Split into query builder, ranker, semantic enrichment, aggregation, and formatter components. | Search behavior is easier to test and tune. | Unit tests per component. |
| TODO-P2-010 | Mobile API Client Size | `apps/mobile/src/api/client.ts` | Mobile client is a very large module with many unrelated domains. | Split into domain clients: auth, listings, bookings, payments, messaging, notifications, organizations, disputes, reviews, uploads. | Smaller modules, easier tests, no circular dependencies. | Module-level client tests. |

---

## P3 — Future Enhancements

| ID | Area | File(s) | Issue | Required Fix | Acceptance Criteria | Required Tests |
|---|---|---|---|---|---|---|
| TODO-P3-001 | Admin UX | Admin routes/screens | Add richer operational dashboards for booking/payment/dispute/notification DLQs after correctness blockers are fixed. | Build data-first admin dashboards with filters, drilldowns, and action audit trails. | Admin can resolve production incidents without DB access. | Admin E2E tests. |
| TODO-P3-002 | Marketplace Trust | Reviews, disputes, insurance, moderation | Improve user-facing trust indicators: identity verified, insured listing, dispute policy, owner response rate. | Add consistent trust badges and explanations. | Renter understands marketplace risk before booking. | UI/accessibility tests. |
| TODO-P3-003 | AI Features | AI description/pricing/search features | Add transparent review/override UI for AI-generated descriptions and price suggestions. | AI output is never silently published without user review. | Owners can accept/edit/reject AI suggestions. | UI/API tests for AI review flow. |

---

## Recommended Execution Order

1. Fix P0 availability false-positive behavior and authorization.
2. Unify booking/availability reservation logic and date overlap semantics.
3. Fix payment webhook canonical ID usage and escrow/deposit idempotency.
4. Fix web/mobile booking API response contract.
5. Fix search cache key collisions and listing static route ordering.
6. Repair env scripts and re-enable critical skipped tests.
7. Add release gate: no critical-path skipped tests, no production bypass flags, no unresolved P0/P1.

---

## Minimum Release Gate After Fixes

- `pnpm run typecheck`
- `pnpm run lint`
- `pnpm run test:unit`
- `pnpm run test:integration`
- `pnpm run test:e2e`
- Critical concurrent booking API E2E
- Payment webhook replay/idempotency E2E
- Web and mobile booking list contract tests
- Search cache/filter integration tests
- Config guardrail tests for production env
- Security tests for listing media and object-level authorization
